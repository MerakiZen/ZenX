# Auth Service

Handles authentication, SSO, and token issuance for ZenX. This gRPC microservice follows the contracts defined in `proto/auth/v1/auth.proto`.

## Run locally

```bash
cp .env.example .env
make dev
```
