# Exercise Service

Stores and searches the exercise library for ZenX. This gRPC microservice follows the contracts defined in `proto/exercise/v1/exercise.proto`.

## Run locally

```bash
cp .env.example .env
make dev
```
