# Analytics Service

Calculates progress metrics and PRs for ZenX. This gRPC microservice follows the contracts defined in `proto/analytics/v1/analytics.proto`.

## Run locally

```bash
cp .env.example .env
make dev
```
