# Workout Service

Owns workout session creation, logging, and streaming for ZenX. This gRPC microservice follows the contracts defined in `proto/workout/v1/workout.proto`.

## Run locally

```bash
cp .env.example .env
make dev
```
