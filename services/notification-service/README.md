# Notification Service

Queues push/email reminders and alerts for ZenX. This gRPC microservice follows the contracts defined in `proto/notification/v1/notification.proto`.

## Run locally

```bash
cp .env.example .env
make dev
```
